{
  "$comment": "0.7.0 daemon architecture: Node spawns laqrumcode-mcp (thin client). mcp-client either connects to an already-running laqrumcode-daemon or forks one detached. Daemon owns SurrealStore + BGE-M3 + all tool/hook handlers. The 'mcp-server.js' legacy single-process entry point is still present in dist/ for emergency rollback (just point command back at it).",
  "mcpServers": {
    "laqrumcode": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/mcp-client/index.js"],
      "alwaysLoad": true
    }
  }
}
