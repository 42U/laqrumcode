node_modules/
.kongcode.sock
.kongcode-port
.kongcode-handoff.json
.orphaned_at

# SurrealQL CLI scratchpad — accumulates working queries from probe/audit
# sessions. Local-only by design; durable queries live in test/integration/
# or saved findings. Untracked via `git rm --cached` 2026-05-18 (W2-7).
history.txt

# Internal upgrade scaffolding (Claude-Code-driven self-upgrade runs).
# Stays local for the dev who's running an upgrade; never ships in releases.
.upgrade/

# Local dev tooling (release scripts, etc.) — not shipped to users.
dev/

# Per-platform SEA binaries (~120MB each, regenerated by 'npm run sea').
# Built in CI for releases; local builds never get committed.
dist/bin/
dist/sea-prep.blob
dist/daemon-sea-prep.blob
dist/mcp-sea-prep.blob
dist/mcp-server.bundle.cjs
dist/mcp-server.bundle.mjs
dist/daemon/bundle.cjs
dist/mcp-client/bundle.cjs

# Environment
.env
.env.local
.env.*.local

# OS / editor
.DS_Store

# Process
*.pid

# Test coverage
coverage/
