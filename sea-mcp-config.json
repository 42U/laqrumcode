{
  "$comment": "SEA config for laqrumcode-mcp — the thin per-Claude-Code-session client. Forwards MCP RPC to laqrumcode-daemon over local IPC. No assets needed (no schema, no DB) — daemon owns all heavy state.",
  "main": "dist/mcp-client/bundle.cjs",
  "output": "dist/mcp-sea-prep.blob",
  "useSnapshot": false,
  "useCodeCache": false
}
